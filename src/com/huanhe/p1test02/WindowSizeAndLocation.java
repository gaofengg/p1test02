package com.huanhe.p1test02;

/*
 * 功能要求：
 * 1、设置窗口的location到屏幕的中心位置的方法
 *     setLocationToCenterOfScreen(Component c)
 * 2、设置窗口位置到指定组件位置的相对位置
 *     setLocationRelativeComponent(Component c, int offsetX, int offsetY)
 * 3、设置窗口最大尺寸为屏幕尺寸，自定义最小尺寸
 *     setMaxAndMinDimension(Component c, int minWidth, int minHeight)
 * 以上方法均为静态方法。
 */
public class WindowSizeAndLocation {
    static void sys() {
        System.out.println("hello");
    }
}
